export interface LivroFilter {
    guuid?: string,
    autor: string,
    ano: number,
    editora: string,
    codigoBarras: number,
    urlCapa?: string,
    titulo: string,
    observacao?: string,
    guuid_Usuario: string
}